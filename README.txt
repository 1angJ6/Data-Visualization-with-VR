http://klen.github.io/py-frameworks-bench/

flask work direction
everything in the python is object, use xxx.copy() to copy a dict rather than just use "=" which just reference to the same object!!!

3.11
object balabala function can be only be called in the main thread